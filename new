if [ "$($docker_path/docker ps -qa -f name=medicalstore)" ]; then
    	if [ "$($docker_path/docker ps -q -f name=medicalstore)" ]; then
        		$docker_path/docker stop medicalstore;
    			 fi
    			   $docker_path/docker rm medicalstore;
			 fi
